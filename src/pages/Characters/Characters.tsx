import { Button, Container, VStack } from '@chakra-ui/react'

export const Characters = () => {
  return (
    <Container p={2}>
      <VStack>
        <Button>Click me</Button>
        <Button>Click me</Button>
      </VStack>
    </Container>
  )
}
